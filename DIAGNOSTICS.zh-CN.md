# 游戏身份与启动诊断

[English](DIAGNOSTICS.md) | **中文**

启动身份与报告从 dev.2 开始提供，本页记录仍适用于 dev.20 的内部报告格式。dev.20 新增公开诊断记录 API 和游戏内工具，见 [DEVELOPER_TOOLS.zh-CN.md](DEVELOPER_TOOLS.zh-CN.md)。报告文件格式本身不属于稳定公开 API；事件时序、取消和异常传播不变。

## 游戏身份

启动层在 Fabric 解析 MOD 依赖之前，通过 ASM 直接读取游戏字节码中的常量字符串字段 `AGame.VERSION`，不加载或初始化游戏类。按实际游戏类路径取首个 `AGame.class`，后续同名定义会产生提示；首个定义缺少版本或不可读时，不借用后续归档的版本。

可识别版本（包括四段版本 `1.2.15.2`）成为 Fabric 内置 `airships` MOD 的版本，参与依赖检查。不符合版本格式的字符串保留为原始值，同时告警并将规范版本回退到 `0.0.0`；无法取得原始值时使用 `unknown`。不根据归档文件名猜测发行版本。

**兼容性变化：** `airships` 不再始终是 `0.0.0`。精确依赖旧占位值的 MOD 现在可能无法通过依赖解析。作者应声明实际验证过的游戏版本，或在有意不限制版本时使用 `*`。框架不会改写依赖声明或绕过 Fabric 的检查。

代码指纹覆盖实际类路径中含有 `com/zarkonnen/airships/` 下 `.class` 文件的每个完整归档，按类路径顺序计算各自的 SHA-256。组合指纹为以下 UTF-8 内容的 SHA-256：前缀 `acbric-game-archives-v1\n`，之后逐个追加小写归档哈希和换行。文件名与路径不参与；顺序和字节变化会改变指纹。即使类内容不变，重新打包 ZIP 也可能改变指纹。没有归档或归档检查失败时返回 `unavailable`，不把部分结果当作完整指纹。

指纹标识代码输入，不代表兼容性。它不覆盖散装资源、全部依赖、MOD 内容、存档或联机行为。

## 报告位置

每次启动生成独立 UUID 会话，保存在 **Fabric 游戏目录**下：

```text
game/logs/acbric/<会话 UUID>/
├── launch.properties
└── startup.json
```

控制台会打印目录，两份报告带相同会话 ID。文件使用 UTF-8，只保存在本地，不上传。旧会话不自动清理；可关闭游戏后手动移除旧目录。内部字段格式和用于关联会话的系统属性都是诊断实现细节，不是稳定的 MOD API。

`launch.properties` 在 Fabric 依赖解析前写入，包含 Java 版本、进程 ID、游戏版本及来源、归档顺序与哈希、指纹、警告和启动阶段：

| 阶段 | 含义 |
| --- | --- |
| `GAME_LOCATED` | 已找到启动输入；后续依赖解析、类变换和 API 初始化仍可能失败 |
| `ENTERING_MAIN` | 即将解析并调用配置中的主类 |
| `MAIN_RETURNED` | 主方法正常返回，不代表所有玩法子系统都成功 |
| `MAIN_FAILED` | 主类解析或调用失败；原始异常继续向外传播 |

`startup.json` 从 Acbric API 进入预启动时开始记录。它列出 Fabric 已加载 MOD 的 ID 和版本，包括 `fabricloader`、`airships`、`acbric_api`。只跟踪 `acbric` 入口，并覆盖入口实例构造失败：

| `acbricStatus` | 含义 |
| --- | --- |
| `NOT_REACHED` | 尚未完成 Acbric 入口发现 |
| `NO_ACBRIC_ENTRYPOINT` | 没有 `acbric` 入口；资源 MOD 和基础设施可以合法处于此状态 |
| `PENDING` | 已发现入口，但尚未全部执行 |
| `RUNNING` | 正在执行入口 |
| `SUCCEEDED` | 该 MOD 的全部 `acbric` 入口正常返回 |
| `FAILED` | 至少一个入口失败，即使其他入口成功也保留失败状态 |

每个入口单独记录定义和状态，失败附带异常类型、消息及堆栈。保留原有逐入口隔离行为：一个入口失败仍会记录日志，之后继续处理其他入口，不回滚已发生的部分修改。

报告阶段区分资源准备、入口初始化、无入口失败的完成、有入口失败的完成，以及预启动致命失败。完成**不代表资源安装全部成功**：已有内嵌资源加载器会自行把可恢复错误输出到控制台。其他 Fabric 入口类型以及 `acbric` 初始化之后的错误不属于此处跟踪范围。

## 如何判断未完成的启动

- 只有 `launch.properties`：检查 Fabric 日志，可能在 API 预启动前遇到依赖或类变换失败。
- 停在 `RUNNING`/`PENDING`：可能是进程中断或回调没有返回；报告只能说明最后持久化的阶段，不能独立判断死锁。
- `acbric` 入口失败未必阻止游戏启动，即使进入主方法也应检查逐 MOD 结果。
- 报告不可写时告警，不改变初始化或启动结果，也不删除挡住目录的现有文件。写入失败可能导致已保存阶段滞后，或没有报告。

报告先写临时文件，再尽可能原子替换；不支持原子移动时使用普通替换。这是诊断快照，不是抗崩溃事务日志。游戏定位之前的错误、Loader 早期失败、其他预启动入口、后台线程错误和强制结束等不能保证全部捕获。排错时同时保留常规控制台/Fabric 日志。堆栈可能包含本地路径和 MOD 自定义消息，分享前自行检查。

## dev.6 运行期事件诊断

受管理 EventScope 回调失败写入同一启动会话的 runtime-events.jsonl。旧注册方式保持。归属、输出上限及异常契约见 [订阅范围手册](EVENT_SCOPES.zh-CN.md)。

## 本地代码清单（dev.7）

acbric 入口处理完成后，同一会话目录新增 `code-manifest.json`。见[覆盖范围、指纹规则与离线比较](CODE_MANIFEST.zh-CN.md)。这不表示所有初始化完成，也不保证联机兼容。
