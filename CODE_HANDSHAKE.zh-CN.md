# 代码握手协议与状态机（dev.8）

[English](CODE_HANDSHAKE.md) | **中文**

本轮实现框架内部的报文和状态管理，并用真实原版 Server、两个独立 Fabric/Client 进程验证。**尚未接入战役大厅 UI、准备按钮或开局流程**；正常启动游戏不会自动运行握手。`CodeHandshakeProtocol`、`CodeHandshakeSession` 是包内实现，不是公开 MOD API。

## 检查的意义

沿用 [代码清单](CODE_MANIFEST.zh-CN.md) 的覆盖范围。`CODE_MATCH` 只表示：本机已收到当前房间每个其他成员对本机当前挑战的有效响应，响应中的代码清单一致，且对方对本机清单的比较状态也一致。它是**本机持有的逐成员证据**，不表示所有客户端已取得全房间共识，也不是可以直接开局的凭证。

原生 Server 不认证自定义消息中的发送者字段；客户端自报 ID、会话及清单。会话随机值用于隔离迟到和重复报文，不提供身份认证或反作弊保证。原生资源、共享配置、存档状态和模拟同步仍不在检查范围内。

## 状态与边界

| 状态 | 含义 |
| --- | --- |
| `IDLE` | 尚未绑定房间，或已断线 |
| `ALONE` | 房间只有本机；没有验证其他玩家 |
| `CHECKING` | 至少一个当前成员仍待响应 |
| `CODE_MATCH` | 当前全部对端已确认代码一致，且证据尚未过期 |
| `DIFFERENT` | 存在可验证的代码差异；保留具体 MOD/字段差异 |
| `UNVERIFIABLE` | 本地/远端清单不可验证、过大，或双方比较结论矛盾 |
| `TIMED_OUT` | 对端未在期限内有效响应；旧框架、报文损坏、协议不支持也可能导致此状态 |
| `CLOSED` | 永久关闭并释放成员记录 |

快照不可变，提供各成员状态、发送次数、最近确认的远端会话及详细比较结果。本地问题优先显示 `UNVERIFIABLE`；否则成员汇总优先级为 `UNVERIFIABLE`、`DIFFERENT`、`TIMED_OUT`、`CHECKING`、`CODE_MATCH`。同时存在多种问题时仍可查看逐成员记录。

- 房间 ID、本机 ID、成员集合变化时，清空全部确认并生成新会话和挑战；只改变成员顺序不重置。
- 断线后重新绑定，即使房间和 ID 相同也生成新会话。原生 Client 内部重连可能保留 ID，适配层必须显式报告断线或调用 `restart`，不能只看成员列表。
- 请求发出后每 2 秒重试，最多 5 次；从本轮成员检查开始计时，10 秒超时。发送失败也占一次尝试；定时器不会补发暂停期间积压的请求。
- 超时后，迟到的响应和请求都不恢复成功；需要显式重试或新的房间/连接上下文。
- 一致证据有效期 30 秒，到期先失效再重新挑战；重复响应不延长有效期。差异/无法验证状态保持阻断语义，直到显式重试或上下文变化。
- 已确认对端发来不同会话的有效请求时，保守地重新检查。一个迟到的旧请求可能触发重查，但不能直接恢复旧的一致结果。
- 单个对端回复频率最多每 250 毫秒一次；每次 `tick` 最多生成每个对端一个请求，没有无界发件队列。

## 报文 v1

原生房间中的普通自定义消息：`type = acbric:code_handshake`、`protocol = 1`。不使用历史记录或全局广播。

| 字段 | 约束 |
| --- | --- |
| `kind` | `request` 或 `response` |
| `channel`、`from`、`to` | 正的房间 ID；非负且不同的发送/接收成员 ID |
| `members` | 当前完整成员集合，最多 32 人，无重复；必须与本机当前集合相同 |
| `session` | 发送方本轮上下文 UUID |
| `requestSession`、`challenge` | 请求方上下文 UUID 和对端挑战 UUID；响应原样回显 |
| `status` | 请求为 `REQUEST`；响应为 `CODE_MATCH` / `DIFFERENT` / `UNVERIFIABLE` |
| `manifest` | 清单 JSON 对象；发送端清单超过预算时明确发送 JSON `null` |
| `###` | 仅允许原生 Client 附加的非负整数传输序列号，不参与应用层确认 |

清单序列化后最多 **32000 UTF-8 字节**，完整协议报文最多 **40000 字节**，为原生 50000 字节发送上限留出余量。超过清单预算时，本地记录 `MANIFEST_TOO_LARGE`，发送小型空清单声明，双方结果均为 `UNVERIFIABLE`；绝不把空值当作无 MOD 或截断后继续比较。离线清单工具仍允许 1 MiB，首版网络协议不分片。

严格拒绝未知字段/版本、重复 JSON 键、错误类型、浮点伪整数、非法 UUID、未知清单格式和超限输入。校验协议数值时保留 JSON 原始类型，不先调用游戏的浮点序列化器。无效、错误收件人、错误房间/成员集合、过期挑战或历史帧中的消息不产生确认；没有有效响应时最终超时，不回退兼容成功。

## 未来适配层必须履行的职责

1. 在同一线程提供单调、非负的毫秒时钟，定期 `tick` 并查询当前快照；暂停后先更新时间，不能沿用旧快照放行。
2. 从当前连接的原生欢迎/成员帧取得上下文，过滤旧连接/旧房间和乱序的外层帧；房间 0 是公共聊天，不启用此检查。成员数超限等上下文错误必须保持未确认。
3. 将消息所属的真实外层房间及历史帧标识传给 `receive`。外层元数据不能从报文的自报字段推断。
4. 将 `tick`/`receive` 返回的文本作为普通房间消息发送，保留原生发送失败结果。不要把报文加到历史记录，也不要全局广播。
5. 只消费框架保留的消息类型；继续交给原生逻辑处理其他消息、成员与房间事件，避免重复轮询 Client 或让扩展报文进入战役指令解释器。
6. 大厅准备和开局还需要独立的协调、失效和二次检查：逐客户端 `CODE_MATCH` 不是全房间确认。房主切换、开始按钮的多入口、读档提前构造世界等问题应在接入时单独解决。

本轮不改变 `LOADED` 时序、不新增游戏逻辑接口、不自动广播战役数据。

## 验证

标准 `gradlew build` 包含 433 项检查，其中本轮 77 项检验握手状态、时钟、重试/超时、重放、成员变动、三成员集合、限额和严格解析。工作区隔离实验以 1.2.15.2、1.2.14 的真实 Server/Client 字节码验证当前产品状态机；测试适配器不是正式大厅接入。两版真实通信共 70 项通过，覆盖代码一致、修改/缺失 MOD、入口失败、超时、房间重入和原生重连。GUI、大厅开局、跨机器、官方服务器和长时间高负载尚未验证。
