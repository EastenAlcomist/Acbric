# 改动记录

## 0.3.3-dev.22 — 外部安装预检查与加载原型（2026-09-26）

- 新增无游戏初始化的预检查入口、Windows Java 引导检查和仅含启动依赖的 `preflightTools`。日志先写临时目录，即使安装/实例路径错误也能定位原因。
- 完整 JSON 解析，显式根目录 A/B、稳定依赖顺序、重复类/关键依赖/版本与代码指纹检查、基础资源目录和 x64 DLL PE 检查；保留旧 steamworks 版本排除规则。
- 外部 Provider 仅做显式路径解析，实例文件锁与写入检查；真实 Knot 探针直接读取安装代码，早期路径 Mixin 分离原生资源根与实例用户数据根。旧启动路径不变，公开 API 签名不变。
- 标准回归 **904 项**（新增 32）；1.2.15.3 外部探针 **25 项**，安装 **5,325 文件内容无变化**；旧布局游戏 1.2.15.2 / 1.2.14 的 ARC 检查 **71×2=142 项**。Windows PowerShell 5.1 下旧 Java 8 被清晰拒绝、Java 21 预检查通过。
- 本轮不调用正常 Main，不覆盖 GPU/原生库实际加载/完整生成/DLC/Workshop/联机，也未重跑此前完整 UI 矩阵。正常外部启动返回 `EXTERNAL_NOT_READY`。资源缓存、LaunchSettings 覆盖、干净发行/模板、迁移和安装器留待后续阶段；现有 distZip 仍非干净框架包。
- 使用与复现见 [外部安装说明](EXTERNAL_INSTALL.zh-CN.md)。dev.22 已按用户要求提交；未推送，未覆盖玩家运行副本。


## 0.3.3-dev.21 — 控制台快捷键（2026-09-26）

从 dev.21 起，按 **Esc 下方、数字 1 左侧的 ~ 键**（物理键 `GRAVE`，可带 Shift）直接打开控制台并聚焦命令框，开窗按键不会混入文本。Ctrl/Alt/Meta 组合、窗口失焦、原生错误/帮助/聊天覆盖层以及已有 Acbric 窗口时不触发；先关闭其他 Acbric 窗口再使用快捷键。控制台打开后该键仍可输入普通字符，用 Esc/X/关闭退出。长按不会反复开窗。这是固定控制台入口，尚未提供通用改键 API。

872 项标准检查及两版 464 项真实 Fabric 检查通过，含 50 项新增快捷键断言。console-demo 0.1.0 与 UI 展示 0.3.0 无需更新。Windows 真实失焦/键盘布局仍需交互验收。


## 0.3.3-dev.20 — 开发者工具与控制台（2026-09-26）

- 集成 API 详情页工具/控制台入口，状态快照、MOD 信息、消息筛选/分页/详情/复制和异步诊断导出。
- 公开命令声明、类型参数、命名空间、帮助、静态补全和注销；执行绑定游戏线程并检查界面/战役，SHARED 预留且始终拒绝。
- 复用已有日志、入口与资源诊断，保留有界消息和原事件异常语义；不截获全部 stdout，不自动同步玩法。
- 文本框新增兼容的 onSubmit，控制台支持 Enter、历史和 Tab 补全；所有新界面和文档同步中英文。
- 统一当前 README/API 与开发导航，另有独立 console-demo 0.1.0，不作为框架默认 MOD。
- 872 项标准及两版 414 项真实 Fabric 检查通过。两个既有符号链接场景跳过；OS/GPU/完整玩法验收仍由实机完成。


dev.19 验证：810 项标准检查（新增 41 项），两版真实 Fabric 各 169 + 5 重启 + 4 损坏配置启动，共 356 项；dev.18 的 80 个公开类型/428 个成员签名保留。剪贴板成功用替身、不可用用真实无头 AWT；原生字体/输入已检查，绘制终端无 GPU，Windows 剪贴板/截图/输入法和失焦仍待实机。以下 dev.18 及更早数字为历史记录。

## 开发版 API 0.3.3-dev.19

dev.19 完善单行输入：原生字体鼠标定位、拖动/Shift 选区、Ctrl+C/X、导航与删除按键重复。设置页固定操作区，并在取消/X/Esc 丢弃草稿前确认；旧构造、程序 close 与生命周期清理保持兼容。配套展示 MOD 0.3.0。详见 [UI 契约](UI.zh-CN.md) 和 [设置契约](SETTINGS.zh-CN.md)。

2026-09-26 实机验收：用户确认 dev.18 测试没有发现问题，并要求提交。提交前全部 28 个变更文件与构建验证快照一致；本次只补充验收说明，沿用 769 项标准检查和两版共 304 项集成检查，不重复测试未改动的代码。未提供逐项实机测试矩阵，此反馈不代表全部 GPU、输入法或第三方 MOD 组合均已覆盖。

最终验证：769 项标准检查（57 项新增）、两版共 304 项真实 Fabric/界面/示例保存/新进程重启及损坏配置启动检查通过，保留 dev.17 的 71 个公开类型/354 个成员。UI 与设置双语文档示例编译检查。绘制终端无 GPU；用户实机反馈见上方，自动化验证不覆盖操作系统显示、输入法和切屏。

## 开发版 API 0.3.3-dev.18

dev.18 新增显式字段的 MOD 设置页、数字/选项组件和受控文本绑定；草稿应用、取消、默认值、重读及双重冲突保护。新增 ModConfig.defaults()/save(expected,data)、ConfigField、ConfigEditor、SettingsUi。生效时机由 MOD 处理，不自动同步配置或修改已有战役。详见 [设置 API](SETTINGS.zh-CN.md)。

2026-09-26 实机反馈：用户确认 dev.17 本轮 UI 修复可以继续推进。本阶段包含公共组件、详情/工具入口、中英文、输入框定位、切屏坐标与关闭字形修复。已有 712 项标准检查、两版共 230 项集成检查；本次提交前 34 个变更文件与已验证快照完全一致，仅补充验收记录，不重复运行未变更代码的测试。此确认不扩大为全部 GPU/输入法/第三方 MOD 组合已验收。

## 开发版 API 0.3.3-dev.17

dev.17 修复新 UI 使用偏移点击坐标导致未命中的问题；原生点击决定触发，Slick 输入使用同帧悬停坐标进行命中，保留缩放、遮蔽及无坐标回退。标题栏关闭改用原生字体支持的 X。日志回放已验证，Windows 切屏仍需实机复测。

## 开发版 API 0.3.3-dev.16

光标误用 MyDraw.tw（大字体加开关按钮装饰宽度），改为和正文相同的 AGame.FOUNT 度量，并让文字及光标一起垂直居中。未更改点击语义或强制重置焦点。最新用户运行日志未见新异常，但不能据此排除事件丢失。新增 acbric-ui-input.log 和 acbric-ui-input.previous.log，位于游戏用户数据目录，每份最多 64 KiB；记录焦点、鼠标按下/释放、原生点击、导航键和框架屏障/布局/命中结果，不记录文本。输入日志写入失败停止诊断，不中断游戏。

## 开发版 API 0.3.3-dev.15

修复点击详情后退回主菜单：模态输入遮蔽将坐标置空，但原生 MyDraw.button 绘制要求非空坐标。提前到 render HEAD、Screen.render 之前恢复绘制坐标与状态，不解除输入阶段的键鼠拦截；覆盖关闭释放帧和设备无坐标的边界。两版均复现 dev.14 同一故障；修复后共 132 项 UI/语言/原生绘制检查通过，实际执行 AirshipGame.render 和 MyDraw.button，仅绘制终端不连接 GPU。标准构建 698 项；真实 GPU 显示仍待验收。

## 开发版 API 0.3.3-dev.14

修复游戏原生 `chi` 中文标签未被识别的问题，同时支持 `zh`/`zho`。新增 `AcbricLanguage`、动态工具入口名称，补齐默认消息按钮与 MOD 信息字段中文。展示 MOD 0.1.1 改为跟随游戏语言，不再读取系统默认语言；大厅文案复用同一判断。新增 8 项语言回归（共 698 项）；两版真实 Fabric 覆盖语言切换及系统/游戏语言不一致。未改变网络协议。

## 开发版 API 0.3.3-dev.13

- 新增公共 UI：标签、按钮、开关、Unicode 单行文本、行列/面板/滚动布局、焦点、提示、模态子窗口和托管清理，见 [UI 契约](UI.zh-CN.md)。
- 框架详情页与 MOD 工具入口共用组件，保留重启启停、行图标和选中反馈；独立展示 MOD 在仓库外。
- 在原生缩放后遮蔽输入，保留游戏 tick；处理打开/关闭点击、滚动后的旧命中、父级禁用、切屏和清理重入，旧事件时序保持。
- 新增 38 项标准检查（690 项总计）；两版真实 Fabric 验证原生 input 循环及缩放、工具工厂、实际详情按钮回调，以及录制绘制器下的裁剪恢复。OpenGL、输入法和完整 GUI 仍待实机验收；未完整重跑此前联机/重启矩阵。

## 开发版 API 0.3.3-dev.12

- 新增 Java MOD 管理：扫描直属 JAR，保留停用条目，分别显示加载、入口失败与待重启状态；行内启停立即保存、下次启动生效，见 [契约](MOD_MANAGEMENT.zh-CN.md)。
- 启动层合并停用配置和外部参数，在 Loader 依赖解析/入口/Mixin 应用前过滤。核心保护、依赖/版本/别名/硬冲突预检查、嵌套元数据检查、配置锁与原子替换；不删除或移动 JAR。
- 按本次启动快照屏蔽有归属记录的配套资源，保留原生偏好和文件；无归属旧目录需先显式迁移。启动器与 API 必须一起更新，旧启动器下界面只读。
- 新增 55 项检查（总计 652）；两版真实 Fabric 重启链验证入口、Mixin、嵌套依赖、列表和原生资源。仍为 21 个 Mixin；未新增稳定公开 API。实际 GUI 绘制/鼠标交互及完整战役仍需验收。


- 两版重启链共 114 项、旧启动器/失败入口/按钮回调共 56 项通过；6 次负向启动符合预期。dev.11 的 57 个公开类型/237 个成员签名保留。

## 开发版 API 0.3.3-dev.11

- 新增只读存档规则报告、注册的直达版本转换、显式缺失规则接纳及不可变预览。只发布新的原生目录存档，不覆盖源或已有目标，见 [契约](RULE_SAVE_MIGRATION.zh-CN.md)。
- 原生选档读取返回后、世界构造前预检查，报告 MOD ID/版本；LOADED 前保留构造检查。加载/序列化不执行迁移回调。新增 1 个 Mixin，共 21 个。
- 保留其他二进制块、MOD 命名空间及缺失 MOD 规则；损坏/不支持格式、源/声明变化、非法结果均拒绝。单文件存档转换暂不支持。
- 新增 56 项（共 597），两版六个真实 Fabric 进程 136 项、四轮双客户端 54 项检查。独立工作区测试 MOD 提供 legacy/v1/v2，不改战斗玩法。完整程序生成世界、GUI 及跨机器验收仍待完成。


## 开发版 API 0.3.3-dev.10

- 新增 `context.sharedRules`、不可变快照和显式候选更新，见 [共享规则](SHARED_RULES.zh-CN.md)。新战役生成前固化已确认值，续局读存档值，不覆盖本地配置。
- 大厅加入规则/字段差异检查，候选变化撤销准备；内部准备元数据 v2 绑定规则摘要。新增 `WorldGenScreenMixin`，共 20 个。
- 保留规则集沿用战役存储管线，LOADED 前拒绝缺失或不兼容的声明规则，保留缺失 MOD 条目，不自动迁移旧档。未接入的旧 MOD 公开接口继续保留。
- 新增 64 项检查（总计 541）、默认未调用的模板示例和双语文档。两版游戏 14 轮真实本机网络实验通过 204 项上层检查；31 类型/125 成员及 dev.6 的 46 类型/185 成员签名保留。
- 原始示例/模板及当前模板真实加载通过。混合夹具曾因另一个射击修正 MOD 的 Steam 不兼容 Redirect 失败，已保留证据，不在本轮修改或宣称兼容。完整 GUI、生成世界及跨机器验收仍待完成。


## 未发布 — API 构建 0.3.3-dev.9

- 接入战役大厅自动代码检查、双语状态/重试按钮，以及原生准备/开局门禁；保留资源、玩家等原生条件，实际动作入口再次复查。见[接入契约](LOBBY_HANDSHAKE.zh-CN.md)。
- 准备凭据与房主更新绑定当前完整成员/会话批次；上下文、重连、设置变化或重试撤销旧准备。例行续证保留未变化身份的准备意愿，全员有效更新提供短期开局窗口。
- 观察原生 socket/重连状态，并处理 ResumeScreen 重建大厅却跳过欢迎入口的交接。支持 LAN 房间 0，保留其他消息，过滤进入战役前后的框架尾包。
- 使用内存启动清单，新增 3 个 Mixin（共 19 个）并扩展恢复钩子。保留公开 MOD API 与存档格式；旧功能 MOD 无需重编译，但同房需框架/代码一致。本轮未增加配置或状态同步。

验证：477 项标准回归（新增大厅 44 项）；两版游戏 1.2.15.2/1.2.14 的 10 组最终原版 Server/双 Fabric Client 实验共 104 项上层检查。原始 31 类型/125 成员及 dev.6 的 46 类型/185 成员保留。探针以最小夹具执行真实大厅方法及变换后的恢复钩子，不覆盖完整 GUI、成功生成世界或官方认证；完整新建/续局和跨机器验收仍待人工完成。

## 0.3.3-dev.8 — 内部握手核心（未发布）

- 新增包内代码握手协议和状态机：上下文/逐成员挑战、成员变化失效、有限重试、明确超时、一致结果到期和不可变快照。见[协议契约](CODE_HANDSHAKE.zh-CN.md)。
- 清单与报文分别限额并低于原生发送上限；超大清单明确无法验证。旧会话、历史帧、错误收件人和未知格式不能回退为成功。
- 协议/清单严格解析保留数值原始类型，先校验再序列化；原配置接口的数值归一化行为不变。
- 尚未接入大厅 UI、准备/开局阻断或自动同步；不新增公开 MOD 接口，不修改 Mixin。

验证：433 项标准回归（新增握手 77 项）；两套游戏 1.2.15.2、1.2.14 的原版 Server 与独立 Fabric 客户端通过实验适配器调用产品核心。两版共 70 项真实通信检查、6 次离线 CLI 检查通过；原始 31 类型/125 成员及 dev.6 的 46 类型/185 成员保留。GUI 和跨机器验收不在本轮范围。

## 0.3.3-dev.7 — 本地代码清单与离线比较，2026-09-25（未发布）

- 根据实际 Loader 根、游戏构建身份与 acbric 入口结果导出本地代码清单；支持嵌套/开发根、内容归一化、有界读取和明确的无法验证状态。
- 新增严格 JSON 离线比较器与 `compareCodeManifests` 任务，区分代码相同、不同、无法验证、无效输入；提供方向明确的差异。见双语[工具手册](CODE_MANIFEST.zh-CN.md)。
- 保留公开 MOD API 和 16 个 mixin；未接入房间握手、准备阻断、共享配置交换或自动同步。导出失败只告警，不改变既有启动语义。

验证：356 项标准回归，新增清单 45 项。两套真实 Fabric/游戏探针分别导出 8/13 个容器，覆盖嵌套、新模板与旧 MOD；各 5 项事件、10 项存储和 31 项生命周期/示例继续通过。另有 6 次 CLI 进程检查覆盖四种退出码与真实清单自比较。原始 31 类型/125 成员及 dev.6 的 46 个公开类型/185 成员保留。未进行真实双客户端通信或完整 GUI 验收。

## 0.3.3-dev.6 — 运行期诊断与订阅管理，2026-09-25（未发布）

- 新增 EventScope 和 context.eventScope(name)，集中注销、一次性消费、旧快照失效与监听器引用释放；应用/战役寿命由 MOD 显式管理。
- 34 个内置事件增加诊断名称。受管理回调失败按 MOD、事件、范围和线程记录到当前启动会话 runtime-events.jsonl；有重复次数、行数和字节限制，诊断失败不掩盖原异常。
- 旧注册方式、顺序、取消及异常传播保持；未自动接管旧监听器。模板改用范围；独立示例 v3.1 增加 F12 检查，存档 schema 3/配置 schema 2 不变。双语契约见 [EVENT_SCOPES](EVENT_SCOPES.zh-CN.md)。

验证：311 项标准回归（新增范围 81 项、诊断 27 项）。两套游戏各通过 5 项真实变换事件诊断、10 项存储和 31 项生命周期/示例检查；1.2.15.2 加载新模板，1.2.14 加载旧 MOD。原始 31 个公开类型/125 成员及 dev.5 的 45 个公开类型/174 成员均保留。部分策略用显式事件派发和最小界面夹具；GUI 和真实双机仍需人工验收。

## 0.3.3-dev.5 — MOD 配置，2026-09-25（未发布）

- 新增 `AcbricModContext.config` 和 `ModConfig/ConfigSnapshot/ConfigException`，提供严格 JSON、命名空间、缺失默认值、验证、显式迁移/重载和独立快照。
- 保存采用锁、磁盘冲突检查、同目录原子替换及一代原始字节备份。损坏/未来版本文件及失败迁移不会静默重置。完整双语[配置契约](CONFIG.zh-CN.md)随分发提供。
- 独立示例升级至 schema 3：F11 重载本地偏好；玩法增量固化进战役。新联机战役及旧存档迁移使用确定默认值，避免各端本地配置分叉。既有公开成员、存储格式和 16 个 mixin 不变。

验证：203 项无界面回归（新增配置 62 项）。两套游戏 1.2.15.2 / 1.2.14 真实 Fabric 各通过 10 项战役存储和 28 项生命周期/v3 示例检查，配置策略部分显式派发事件；1.2.14 同时加载旧 MOD。原始 31 个公开类型、125 个成员/描述符保留。证据位于工作区研究目录。GUI 和真实双机仍待人工验收；dev.4 用户反馈不等于 dev.5 验收。

## 0.3.3-dev.4 — 战役生命周期，2026-09-25（未发布）

- 新增 `AirshipsCampaignEvents.CREATED/LOADED/RESTORED/EXITED`。创建准备早于初始自动保存/校验快照，加载在扩展数据恢复后，状态恢复携带新旧对象且不重播初始化。
- 每客户端跟踪活动战役；菜单/大厅切换和正常退出释放引用，临时界面及恢复等待不判为退出。完整时序见双语[生命周期契约](CAMPAIGN_LIFECYCLE.zh-CN.md)，随分发提供。
- 工作区另建独立存档示例，提供 schema 1/2 构建、显式迁移、只读恢复和地图界面测试操作，未内置到框架或默认 MOD。原存档格式与既有公开成员保持。

验证：标准回归 141 项。两套游戏真实加载各通过原 10 项存档检查及携带 v2 示例的 18 项生命周期/示例检查；v1 示例探针通过 16 项。恢复检查使用已变换的 RETURN handler 与最小界面夹具，不是完整真实网络恢复。完整新战役生成、GUI 自动保存和双机验收仍待人工完成。

用户测试 dev.4 后反馈“暂时没有发现问题”；未提供逐项清单，不据此认定全部联机/恢复场景已验收。

## 0.3.3-dev.3 — 战役数据接口，2026-09-25（未发布）

- 新增 `AcbricModContext.campaignData(worldMap)` 和 `net.fabricacs.api.save`，按 MOD ID 隔离战役 JSON 数据。读取返回独立快照，写入验证并复制；显式迁移成功且结果合法后才提交。拒绝降级覆盖，保留缺失 MOD 的数据。
- 接入 `WorldMap` 原生序列化/恢复，覆盖磁盘存档和状态恢复。使用独立无版本缓存块，让地图 age 不变的修改也进入校验；块内 JSON 文本支持原版二进制/哈希不支持的 null。登记延迟写入时捕获固定快照。
- 无扩展的旧存档按空容器读取；已声明的块缺失、损坏或格式不支持时中止加载，不替换为默认数据。保存/校验不执行迁移回调，写入不广播网络指令。
- 新增中英文[战役数据手册](CAMPAIGN_DATA.zh-CN.md)，模板增加手动调用的 `CampaignDataExample`，最低依赖升至 `>=0.3.3-dev.3`。旧公开成员和事件语义保留；不包含城市升级、自动对象属性或网络指令接口。

验证：标准无界面回归共 131 项（新增战役检查 31 项）。Steam `1.2.15.2`、原包 `1.2.14` 的真实 Fabric 探针各新增通过 10 项战役检查，覆盖变换后的地图构造、原生二进制读写、`CampaignWorld` 往返、实际 `StoredState` 恢复、age 不变时哈希变化及旧快照保持。原有每套 18 个目标类和 22 种面板事件组合也继续通过。用户已确认 dev.1、dev.2 运行正常；dev.3 尚待完整玩法及双机联机/重连验收。

## 0.3.3-dev.2 — 框架内部诊断，2026-09-25

- 依赖解析前从字节码读取 `AGame.VERSION`，不初始化游戏类；可识别的四段版本也直接交给 Fabric。无法识别时保留原始字符串，告警并回退规范版本 `0.0.0`。
- 按实际类路径顺序计算游戏代码归档 SHA-256，记录组合指纹、各归档哈希、版本来源及重复类定义。指纹用于区分构建，不代表兼容性。
- 每次启动在 `game/logs/acbric/<UUID>/` 保存启动与入口报告，区分 Loader 已加载、无 Acbric 入口、等待、执行中、成功、失败；同一 MOD 部分入口失败不会被其他入口成功覆盖，保留逐入口异常堆栈。
- 保留入口失败后续行及主方法失败的异常传播；报告不可写仅告警，不阻止初始化。不新增公开 MOD API、城市升级功能或通用配置服务。
- 分发补入英文 API 手册及双语诊断文档。报告阶段、字段及边界见 [DIAGNOSTICS.zh-CN.md](DIAGNOSTICS.zh-CN.md)。

**依赖兼容变化：** 精确依赖原 `airships =0.0.0` 占位值的 MOD 现在可能无法通过 Fabric 检查；应声明实际验证版本，或在有意不限制时使用 `*`。不绕过依赖检查。公开接口签名与事件行为不变，模板最低 API 依赖仍为 `>=0.3.3-dev.1`。

验证：标准构建通过 100 项无界面回归（新增 20 项）；旧基线 31 个公共类型的 125 个成员/描述符继续保留。Steam `1.2.15.2` 混合库布局、原包 `1.2.14` 分离库布局及其已有 MOD 的真实 Fabric/Mixin 探针均通过，每组加载 18 个目标类并覆盖 22 种重命名面板组合。真实入口构造失败后下一个入口仍执行；故意失败的主方法保留非零退出和 `MAIN_FAILED` 报告。全部使用隔离用户数据，不等于完整 GUI、对局、存档和联机验收；此前人工运行反馈只适用于 `0.3.3-dev.1`。

## 0.3.3-dev.1 — 开发版，2026-09-24

相对 `fa8a100` 的累计维护：修复加载诊断、资源更新和事件订阅问题，保留旧 MOD 的公开接口，新增准确命名的重命名面板事件。此版本尚未作为稳定版发布；本次提交汇总三个修复批次及文档、注释维护。

### 修复与行为变化

| 范围 | 原问题 | 当前行为 |
| --- | --- | --- |
| 数据加载（F01） | 清理真实诊断或将部分失败改成成功 | 保留游戏日志与原始返回值，`DATA_LOADED` 如实传递结果 |
| 资源边界（F02） | 内嵌资源路径可能越出目标目录 | 校验全部条目、目标和祖先链接，暂存后发布；失败不留下部分安装 |
| 类加载（F03） | Loader/Mixin/ASM 同时进入父加载器和游戏加载器 | 分发分离 `loader-libs` 与游戏 `libs`；旧布局按归档类内容排除启动基础设施 |
| 一次性事件（F04） | 代理注销和重复分发有误 | 原子地消费一次性资格，兼容递归、并发和旧快照；再次分发返回中性结果 |
| 资源更新（F05） | 已存在目录直接跳过，更新和重试不完整 | 按 SHA-256 归属更新未修改文件，保留用户改动，备份并恢复中断事务 |
| 面板事件（F06） | ONE_SHOT 名称与真实挂钩面板不符 | 旧钩子原位保留并弃用，新增独立 `RENAME_SHIP_*` 事件，明确混用顺序 |
| MOD 安装（F07） | 有元数据文件就被视为可安装 | 使用 Loader 解析器校验暂存副本，拒绝非法元数据和同名覆盖 |
| MOD 列表（F08） | 嵌套来源调用不支持的路径接口，可能中断列表 | 单独显示父 MOD/包内路径，隔离行失败，重复刷新不累积合成行 |
| 启动分发（F09） | 分发缺少启动脚本 | 增加 `run.bat`，使用内置 JRE 和独立启动库目录，分发携带 LICENSE 与文档 |
| 构建覆盖（F10） | 标准 build 未包含 API 构建 | assemble 包含 API JAR，check 包含无界面回归 |
| 编译环境（F11） | 编码/JDK 设置依赖某台机器 | 明确 UTF-8、框架 Java 21 输出和运行依赖，移除机器专属 JDK 路径 |
| 重复订阅（F12） | 句柄不能区分同一监听器的多次注册 | 每次注册有独立身份，句柄只注销对应订阅，可重复注销 |

资源迁移不会自动发生：无归属旧目录需关闭游戏后显式迁移，先保留备份，只接管内容相同的文件。备份不会自动清理，移除 Java MOD 也不会自动移除已解包资源。详情见 [BUNDLED_RESOURCES.md](BUNDLED_RESOURCES.md)。

### API 与兼容性

- 保留旧公共成员的名称、类型与 JVM 描述符；部分 Event 方法增加同步。31 个基线公共类型、125 个既有成员的对照通过。
- 新增四个 `RENAME_SHIP_*` 字段及 `CombatUiPanelType.RENAME_SHIP`，旧 12 个 ONE_SHOT 字段和三个枚举常量标记弃用，不删除、不别名转发、不更换真实挂钩位置。
- 旧 BEFORE 先执行，再新 BEFORE、原方法、旧 AFTER、新 AFTER；取消时停止后续组、原方法及全部 AFTER。
- 新模板要求 `acbric_api >=0.3.3-dev.1`，包含新的面板事件示例。引用新字段的 MOD 不能运行在旧 0.3.2 API 上。
- 普通监听器的快照语义及异常传播策略保持：旧快照可能继续调用已注销普通监听器；异常阻止后续分发。一次性代理另行保证最多执行一次。

恢复真实加载错误可能让之前因错误被吞掉而“能运行”的数据包出现加载失败；应修正数据包，而不是再次屏蔽错误。公共签名兼容也不能代替第三方 Mixin 和游戏版本兼容性验证。

### 文档与中文注释

- 新增 [API 开发手册](API.zh-CN.md)，覆盖初始化、上下文/路径/日志、事件、兼容迁移、资源及当前限制。
- 更新中英文 README、模板说明、英文 CHANGELOG 和维护指引；明确游戏字节码/资源、Java MOD 目录和原版用户数据目录的区别。
- 为框架、API、模板和回归检查共 43 个 Java 文件补齐中文文件头，维护关键接口、算法分支和 Mixin 时机注释；自有构建/启动脚本增加中文说明。
- 保留自动生成的 Gradle wrapper、第三方许可证和 JSON 配置格式；不向 JSON 注入注释。注释维护不更改运行逻辑。

### 验证与人工测试

- 标准 `build`：80 项无界面回归断言通过；本机符号链接创建权限不足的检查报告跳过，另有 Windows junction 边界验证。
- 框架分发、独立 MOD 模板构建通过；模板默认 `test` 的 NO-SOURCE 不作为测试覆盖。
- 两套本地游戏字节码分别通过真实 Knot/Mixin 加载、18 个目标类转换及每套 22 种重命名面板新旧事件组合检查。
- 四个旧示例的基本加载、新模板的新事件回调、嵌套列表、安装校验及数据错误诊断通过局部集成检查。
- 从原包重新解压并替换框架的独立运行环境，经本地人工测试反馈“运行正常、表现与原包基本一致”。未取得逐场景清单及完整 MOD 清单，不推断全部存档、联机和第三方 MOD 已通过。

### 当前仍有的限制

不提供热加载或完整禁用/卸载；`disabledMods` 尚无实现。嵌套/目录来源的内嵌原版资源不受支持。安装校验不执行依赖解析或 Mixin 预演。实际武器、浮力、动力动作钩子不在本轮范围内。GameProvider 的游戏版本仍为占位值，升级游戏后必须重新核对注入点。
